import React, { FC } from "react";
import styled from "styled-components/native";
import { GestureDetector } from "react-native-gesture-handler";

export const StyledGestureDetector = styled(GestureDetector)`
  flex: 1;
  width: 100%;
`;

